package pp.game.activities;

import pp.game.*;
import android.app.*;
import android.os.*;

public class MainMenuActivity extends Activity {
	public MainMenuActivity() {
		
	}
	
	@Override
	protected void onCreate(Bundle savedInstanceState) {
		super.onCreate(savedInstanceState);
		setContentView(R.layout.main_menu_activity);		
		
		
//		// Setting gradient foreground for main menu items
//		Shader foreground = 	
//		
//		TextView view;
//		view = (TextView)findViewById(R.id.exitBtn);
//		view.getPaint().setShader(foreground);
//		view = (TextView)findViewById(R.id.highScoresBtn);
//		view.getPaint().setShader(foreground);
//		view = (TextView)findViewById(R.id.newGameBtn);
//		view.getPaint().setShader(foreground);
	}
}
